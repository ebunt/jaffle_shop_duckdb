models:
  - name: metricflow_time_spine
    columns:
      - name: date_day
        description: A single date column used by MetricFlow as the time spine.
        granularity: day
    time_spine:
      standard_granularity_column: date_day
